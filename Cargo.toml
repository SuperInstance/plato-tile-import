[package]
name = "plato-tile-import"
version = "0.1.0"
edition = "2021"
description = "High-throughput tile import pipeline — batch validation, schema checking, dedup, transform"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
