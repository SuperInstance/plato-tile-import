[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-import"
version = "0.1.0"
description = "Tile import/export — Markdown, JSON, CSV, plaintext"
license = {text = "MIT"}
requires-python = ">=3.8"
